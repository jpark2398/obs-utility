[project]
name = "obs-utility"
version = "0.1.0"
description = "Utility to help route PipeWire audio streams into OBS automatically."
requires-python = ">=3.8"

[project.optional-dependencies]
dev = ["pytest"]
